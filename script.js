const button = document.getElementById("button");

button.addEventListener("click", roll);

/*function roll() {
    const firstdie= document.getElementById("First Dice");
    const seconddie= document.getElementById("Second Dice");
    const sum = document.getElementById("sum");
    let first= Math.floor(Math.random() * (6) + 1);
    let second= Math.floor(Math.random() * (6) + 1);
    let total= first+second; 
    firstdie.textContent = `First die: ${first}`;
    seconddie.textContent= `Second die: ${second}`;
    sum.textContent=`Total: ${total}`;
  }
  */